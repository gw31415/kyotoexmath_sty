\usepackage{fancyhdr}
\newcommand{\firstemptypage}{
\newpage
\thispagestyle{empty}
　%←全角スペース
\newpage
}
\newcommand{\scoresum}{200}
\newcommand{\setscoresum}[1]{\renewcommand{\scoresum}{#1}}
\newcommand{\footmark}{\today}%ページ右下の記述
\newcommand{\setfootmark}[1]{\renewcommand{\footmark}{#1}}
\newcommand{\sbjname}{数 \hspace{2cm} 学}
\newcommand{\subject}[1]{\renewcommand{\sbjname}{#1}}
\newcommand{\titlename}{京 大 模 試}
\renewcommand{\title}[1]{\renewcommand{\titlename}{#1}}
\newcommand{\prereadme}{配点は, 学生募集要項に記載のとおり.}
\newcommand{\setprereadme}[1]{\renewcommand{\prereadme}{#1}}
\newcommand{\probspace}{3cm}
\newcommand{\setprobspace}[1]{\renewcommand{\probspace}{#1}}
\newcommand{\probends}{
  \vfill
  \begin{center}
  \bf\Large 問題は, このページで終わりである.
  \end{center}
}

\fancypagestyle{dp}
{
  \lhead{}
  \rhead{}
  \cfoot{}
  \rfoot{\normalsize ◇\it \footmark}
  \renewcommand{\headrulewidth}{0.0pt}
}
\fancypagestyle{pg}
{
  \lhead{}
  \rhead{}
  \cfoot{---\hspace{1em}\thepage\hspace{1em}---}
  \rfoot{\normalsize ◇\it \footmark}
  \renewcommand{\headrulewidth}{0.0pt}
}
\newcounter{probnum}
\setcounter{probnum}{0}
\newenvironment{prob}[1]
{
  \stepcounter{probnum}
  \noindent{\sf\Large\setlength{\fboxsep}{0.7ex} \fbox{\the\value{probnum}}}\rightline{(#1点)}

  \begingroup
  \leftskip2em
  \rightskip\leftskip
}
{
  \par
  \endgroup
  \vspace{\probspace}
}

\newcommand{\readme}{
  \begin{enumerate}
    \item 解答用紙は事前に用意すること.
    \item 電卓等の使用は禁止する.
    \item 解答のための下書き, 計算などは, 計算用紙を用意して書いてもよい.
    \item 解答に関係のないことを書いた答案は無効にすることがある.
    \item 途中経過は加点対象にすることがある.
    \item 解答は分断を避けること.
    \item 問題冊子は持ち帰ってもよい.
  \end{enumerate}
}
\newcommand{\setreadme}[1]{\renewcommand{\readme}{#1}}
\renewcommand{\maketitle}{
\pagestyle{dp}
\large
\begin{center}
{\LARGE \titlename}\\
\vspace{2cm}
{\Huge \sbjname}\\
\vspace{2cm}
{\scoresum 点満点\\ \(\ll\) \prereadme \(\gg\)}
\\
\vspace{0.5cm}
\flushleft \bf (注　意)

\normalfont
{
  \readme
}
\end{center}
\newpage
\pagestyle{pg}
\setcounter{page}{1}
}
